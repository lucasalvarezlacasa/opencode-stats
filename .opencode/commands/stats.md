---
description: Show full cost, token, model, tool, and subagent stats for the current session
---

Call the `session_stats` tool for the current opencode session.

Report the result directly. If the tool output is long, keep the totals and the highest-cost model/tool sections visible, but do not omit errors.
